[build]
builder = "nixpacks"
buildCommand = "rm -rf node_modules && npm ci --production"

[deploy]
startCommand = "node --max-old-space-size=512 index.js"
healthcheckPath = "/health"
healthcheckTimeout = 5

[deploy.env]
NODE_ENV = "production"